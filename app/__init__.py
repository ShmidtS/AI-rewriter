"""
Application entry points for AI Book Rewriter.

This package provides unified launchers for different interfaces:
- python -m app.web  — launch web interface
- python -m app.gui  — launch GUI interface
- python -m app.cli  — launch CLI interface (placeholder)

The main.py entry point uses these launchers internally.
"""

__version__ = "1.0.0"
